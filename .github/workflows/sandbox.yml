name: Sandbox (Node)

on:
  workflow_dispatch:
    inputs:
      enable-test:
        type: boolean
        default: false
      enable-sonar:
        type: boolean
        default: false
      enable-snyk:
        type: boolean
        default: false

jobs:
  
  pre-build:
    name: Pre-Build Validation
    uses: D2nke/actions/pre-build@feat/new_code_base

  node-sandbox-build:
    name: "🚀 Node Build Sandbox"
    uses: D2nke/my_workflows/.github/workflows/node_build.yml@feat/new_base
    with:
      enable-test: ${{ inputs.enable-test }}
      enable-sonar: ${{ inputs.enable-sonar }}
      enable-snyk: ${{ inputs.enable-snyk }}
    secrets: inherit

  # node-sandbox-deploy:
  #   name: "🚀 Node Deploy to Sandbox"
  #   uses: D2nke/my_workflows/.github/workflows/node_sandbox_deploy.yml@feat/new_base
  #   secrets: inherit
